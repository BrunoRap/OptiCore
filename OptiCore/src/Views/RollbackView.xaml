<UserControl x:Class="OptiCore.Views.RollbackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource Rollback_Title}" FontFamily="Segoe UI Semibold" FontSize="20" Foreground="#e6edf3" VerticalAlignment="Center"/>
            <Button x:Name="RefreshButton" Content="{DynamicResource Rollback_RefreshButton}" Style="{StaticResource SecondaryButton}"
                    HorizontalAlignment="Right" Click="RefreshButton_Click"/>
        </Grid>

        <TextBlock Grid.Row="1" Text="{DynamicResource Rollback_Desc}"
                   FontFamily="Segoe UI" FontSize="12" Foreground="#8b949e" Margin="0,0,0,12" TextWrapping="Wrap"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="BackupsPanel"/>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="RestoreSelectedButton" Content="{DynamicResource Rollback_SelectedButton}" Style="{StaticResource SecondaryButton}"
                    Margin="0,0,8,0" IsEnabled="False" Click="RestoreSelectedButton_Click"/>
            <Button x:Name="RestoreAllButton" Content="{DynamicResource Rollback_AllButton}" Style="{StaticResource PrimaryButton}"
                    IsEnabled="False" Click="RestoreAllButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
