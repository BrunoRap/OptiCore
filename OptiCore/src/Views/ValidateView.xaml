<UserControl x:Class="OptiCore.Views.ValidateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource Validate_Title}" FontFamily="Segoe UI Semibold" FontSize="20" Foreground="#e6edf3" VerticalAlignment="Center"/>
            <Button x:Name="RunButton" Content="{DynamicResource Validate_RunButton}" Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Right" Click="RunButton_Click"/>
        </Grid>

        <!-- Summary -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,12" x:Name="SummaryBorder" Visibility="Collapsed">
            <StackPanel>
                <TextBlock x:Name="SummaryText" FontFamily="Segoe UI Semibold" FontSize="14" Foreground="#e6edf3"/>
                <ProgressBar x:Name="SummaryBar" Style="{StaticResource AccentProgressBar}" Margin="0,8,0,0" Height="8"/>
                <TextBlock x:Name="LastValidated" FontFamily="Segoe UI" FontSize="11" Foreground="#8b949e" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Loading -->
        <TextBlock Grid.Row="2" x:Name="LoadingText" Text="Running validation checks..." Visibility="Collapsed"
                   FontFamily="Segoe UI" FontSize="13" Foreground="#8b949e" Margin="0,0,0,12"/>

        <!-- Results -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ResultsPanel"/>
        </ScrollViewer>
    </Grid>
</UserControl>
